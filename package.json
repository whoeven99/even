{
  "name": "even",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -k -n server,client -c green,blue \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
